import {css} from 'lit';

export default css`
    .umoja-l-input-group__item{
        display: grid;
        grid-template-columns: 1rem auto;
        align-items: center;
        grid-column-gap: 0.5rem;
        position: relative;
    }
`;